## Developing

### Installing dependencies

#### Nix flake + direnv
Add to `.envrc`:

``` shell
use flake
```

and run

``` shell
direnv allow
```

### General

Start a development server:

``` shell
npm run dev
```
